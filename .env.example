# Postgres
POSTGRES_USER=txn_user
POSTGRES_PASSWORD=txn_password
POSTGRES_DB=txn_pipeline

# Used later once SQLAlchemy is wired up
DATABASE_URL=postgresql+asyncpg://txn_user:txn_password@postgres:5432/txn_pipeline

# Celery / Redis
CELERY_BROKER_URL=redis://redis:6379/0
CELERY_RESULT_BACKEND=redis://redis:6379/0

# LLM (used later, in the classification + narrative chunk)
GEMINI_API_KEY=your_gemini_api_key_here
