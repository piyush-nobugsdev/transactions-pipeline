# Modules package

This package holds feature modules.

Each module owns:

- controllers
- application services and DTOs
- domain entities, value objects, and repository contracts
- infrastructure implementations
- tests and docs
